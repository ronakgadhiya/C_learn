#include <stdio.h>

void main(){
    int a, b, result, multiple, subtract;
    printf("Please Enter the numbers:");
    scanf("%d%d", &a, &b);

    result = a+b;
    multiple = a*b;
    subtract = a-b;
printf("The sum of the number is: %d \n The multiple of the number is: %d \n The subtraction of the number is: %d",result, multiple, subtract);
    

}